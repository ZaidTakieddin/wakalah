# Production dependencies for hosting (Render free tier).
# Mirrors [project].dependencies in pyproject.toml — keep the two in sync.
fastapi
uvicorn[standard]
pydantic>=2
httpx
python-dotenv
supabase
langgraph
pydantic-ai
