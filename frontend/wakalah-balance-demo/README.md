# WAKALAH Balance Demo

A simple one-page transfer demo connected to the WAKALAH backend.

## Run

1. Copy `.env.example` to `.env.local`.
2. Run `npm install`.
3. Run `npm run dev`.

The recipient field lets you select one of the three example recipients or type a new name. WAKALAH receives whether the recipient is known or new.
